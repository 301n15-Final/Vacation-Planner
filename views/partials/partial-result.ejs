<h2 id="trip-name">Your trip to <%= request.city %> <br>
  <%= request.start_date %> to <%= request.end_date %></h2>

  <h3>Information about your destination:</h3>

  <div class="country-info flex-row">
    <img src="<%= country.flag_url %>" alt="flag of <%= country.country %>">
    <ul>
      <li><b>Country: </b><%= country.country %></li>
      <li><b>Capital: </b><%= country.capital %> </li>
      <li><b>Population: </b><%= country.population %></li>
      <li><b>Borders: </b><%= country.borders %></li>
      <li><b>Currencies: </b><%= country.currencies %></li>
      <li><b>Languages: </b><%= country.languages %></li>
    </ul>
  </div>

  <h3>For the dates of your trip, we expect:</h3>
  <ul class="forecast-list scroll-x">
    <% weather.forEach( day => { %>
      <section>
        <div class="date-temp-icon">
          <li><img class="icon" src="<%= day.icon_url %>" alt="weather icon"> </li>
          <div>
            <li><%= day.day %> </li>
            <li>Average temperature: <%= day.temperature %> &deg;F</li>
          </div>
        </div>
        <li><%= day.summary %></li>
      </section>
      <% }) %>
  </ul>

  <h3>For your trip we suggest you to bring:</h3>
<ul class="items scroll-x">
  <% items.forEach( item => { %>
  <li><input type="checkbox"> <%= item %></li>
  <% }) %>
</ul>